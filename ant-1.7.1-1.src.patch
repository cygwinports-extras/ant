--- origsrc/apache-ant-1.7.1/src/script/ant	2008-06-27 00:05:19.000000000 -0500
+++ src/apache-ant-1.7.1/src/script/ant	2009-12-13 21:18:50.987408300 -0600
@@ -81,7 +81,6 @@ fi
 cygwin=false;
 darwin=false;
 case "`uname`" in
-  CYGWIN*) cygwin=true ;;
   Darwin*) darwin=true
            if [ -z "$JAVA_HOME" ] ; then
              JAVA_HOME=/System/Library/Frameworks/JavaVM.framework/Home
@@ -187,10 +186,10 @@ if $rpm_mode && [ -x /usr/bin/build-clas
 
   # Explicitly add javac path to classpath, assume JAVA_HOME set
   # properly in rpm mode
-  if [ -f "$JAVA_HOME/lib/tools.jar" ] ; then
+  if [ -e "$JAVA_HOME/lib/tools.jar" ] ; then
     LOCALCLASSPATH="$LOCALCLASSPATH:$JAVA_HOME/lib/tools.jar"
   fi
-  if [ -f "$JAVA_HOME/lib/classes.zip" ] ; then
+  if [ -e "$JAVA_HOME/lib/classes.zip" ] ; then
     LOCALCLASSPATH="$LOCALCLASSPATH:$JAVA_HOME/lib/classes.zip"
   fi
 
--- origsrc/apache-ant-1.7.1/src/script/runant.pl	2008-06-27 00:05:15.000000000 -0500
+++ src/apache-ant-1.7.1/src/script/runant.pl	2009-11-30 02:31:04.548037400 -0600
@@ -72,7 +72,7 @@ if ($^O eq "NetWare")
   $onnetware = 1;
 }
 
-my $oncygwin = ($^O eq "cygwin");
+my $oncygwin = 0;
 
 #ISSUE: what java wants to split up classpath varies from platform to platform 
 #and perl is not too hot at hinting which box it is on.
